{
  "_notes": [
    "Copy the section matching your operating system into your claude_desktop_config.json",
    "Remove the other section(s) after merging so only one cheestard-terminal-interactive entry remains"
  ],
  "macOS_linux": {
    "mcpServers": {
      "cheestard-terminal-interactive": {
        "command": "npx",
        "args": [
          "-y",
          "cheestard-terminal-interactive"
        ],
        "env": {
          "MAX_BUFFER_SIZE": "10000",
          "SESSION_TIMEOUT": "86400000",
          "COMPACT_ANIMATIONS": "true",
          "ANIMATION_THROTTLE_MS": "100"
        }
      }
    }
  },
  "windows": {
    "mcpServers": {
      "cheestard-terminal-interactive": {
        "command": "cmd",
        "args": [
          "/c",
          "npx",
          "-y",
          "cheestard-terminal-interactive"
        ],
        "env": {
          "MAX_BUFFER_SIZE": "10000",
          "SESSION_TIMEOUT": "86400000",
          "COMPACT_ANIMATIONS": "true",
          "ANIMATION_THROTTLE_MS": "100"
        }
      }
    }
  }
}
